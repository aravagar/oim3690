# Week 01 Log

Week: 05/18 - 05/24

> **What this is**: a short weekly summary of what you built, learned, and how you used AI. Not a diary — just structured evidence of your work.
>
> **Format**: use proper Markdown — headings, bullet points, code blocks. This file will be rendered on GitHub. If it looks like plain text with no formatting, you're doing it wrong.
>
> **Required**: all 4 sections present and non-empty. Empty sections = incomplete log.

## What I worked on

- Set up GitHub account and created two repositories: `aravagar.github.io` and `oim3690`
- Configured GitHub Desktop and cloned both repos locally
- Created folder structure for course work (`logs/wk01.md`)
- Built and deployed my personal website at [aravagar.github.io](https://aravagar.github.io) using AI-generated HTML
- Published both repos publicly on GitHub

## What I learned

### GitHub and Version Control

- Difference between creating a repo locally via GitHub Desktop vs. on GitHub.com directly
- How to commit and push changes using GitHub Desktop
- Why repos should not be stored in OneDrive or Dropbox

### GitHub Pages

- How GitHub Pages works: naming the repo `username.github.io` is what triggers deployment
- Deployment takes a few minutes after the first push
- The live site URL is different from the file view URL on GitHub

### Markdown

- Markdown is the standard format for logs, READMEs, and documentation
- Basic syntax: headings with `#`, bold with `**`, bullet points with `-`, code with backticks
- GitHub renders `.md` files automatically

### AI-Assisted Development

- How to write a structured prompt to generate a complete `index.html`
- The importance of understanding AI-generated code, not just pasting it

## AI interactions

### Interaction 1

- **What I asked**: Generate a personal website `index.html` based on my background as a Babson student running three family businesses (Realply, Studioforma, Decoven), with interests in regenerative agriculture, Middle Eastern literature, and fitness
- **What AI gave me**: A complete single-file HTML website with dark minimal design, fixed nav, hero section, about section with stat cards, ventures section for all three brands, interests section, and contact section with fade-in scroll animations
- **What I kept / changed / rejected**: Kept the full design and structure as generated. Will iterate on copy and styling in future weeks.

### Interaction 2

- **What I asked**: Help setting up GitHub repos, understanding the publish flow, and troubleshooting why my site was not showing live
- **What AI gave me**: Step-by-step guidance on GitHub Desktop, repo naming conventions, and how to check GitHub Pages deployment status
- **What I kept / changed / rejected**: Followed the instructions directly. The issue was that I was looking at the GitHub file view URL instead of the actual live site URL.

## Questions

- How do I edit specific parts of the HTML without breaking the rest of the file?
- When should I use multiple files vs. keeping everything in one `index.html`?
- How does Live Server work and does it auto-refresh when I save?
- What makes a GitHub commit message good vs. bad?
- What is the difference between a div and a semantic tag?

## Questions from Reading HTML

- What does `<meta charset="UTF-8">` do and why is it in the `<head>` and not the `<body>`?
- What is the purpose of `<meta name="viewport" content="width=device-width, initial-scale=1.0">` in my file?
- Why does the `<link>` tag in the `<head>` point to Google Fonts and what happens if I remove it?
- What does the `lang="en"` attribute on the `<html>` tag do?
- Why are some tags like `<meta>` and `<link>` self-closing while others like `<div>` need a closing tag?
